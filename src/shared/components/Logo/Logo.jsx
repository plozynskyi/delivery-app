import { LogoWrapper, LogoTitle } from './Logo.styled';

const Logo = () => {
  return (
    <LogoWrapper to="/" aria-label="Site logo">
      <LogoTitle>DELIVERY</LogoTitle>
    </LogoWrapper>
  );
};

export default Logo;
